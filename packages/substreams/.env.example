# JWT from https://thegraph.market (Dashboard -> API keys). Needed to stream.
SUBSTREAMS_API_TOKEN=
# substreams.dev registry token. Only needed to publish.
SUBSTREAMS_REGISTRY_TOKEN=
# Archive-capable Ethereum JSON-RPC used by scripts/crosscheck.py for the independent convertToAssets() calls.
ETH_RPC_URL=
